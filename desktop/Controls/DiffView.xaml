<UserControl x:Class="LexWolf.Controls.DiffView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:LexWolf.Services"
             Width="800" Height="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <DockPanel Background="#161b22" Padding="10,5" Grid.Row="0">
            <TextBlock Text="🔍 Diff-Ansicht" 
                       Foreground="#c9d1d9" 
                       FontSize="14" 
                       FontWeight="Bold"/>
            <Button Content="🔄 Diff berechnen" 
                    DockPanel.Dock="Right"
                    Click="OnComputeDiff"
                    Background="#21262d" 
                    Foreground="#c9d1d9" 
                    BorderBrush="#30363d" 
                    BorderThickness="1"
                    Padding="10,5"
                    Margin="10,0,0,0"/>
        </DockPanel>

        <!-- Diff Display (Scrollable) -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <FlowDocumentReader>
                <FlowDocument x:Name="DiffDocument" PageWidth="760">
                    <Paragraph>
                        <Run Text="Klicken Sie auf 'Diff berechnen' um Original und Anonymisiert zu vergleichen." 
                             Foreground="#8b949e"/>
                    </Paragraph>
                </FlowDocument>
            </FlowDocumentReader>
        </ScrollViewer>
    </Grid>
</UserControl>
