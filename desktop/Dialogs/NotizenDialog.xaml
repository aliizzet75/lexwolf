<Window x:Class="LexWolf.Dialogs.NotizenDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LexWolf — Notizen"
        Icon="/Assets/lexwolf.ico"
        Width="780" Height="520"
        MinWidth="560" MinHeight="360"
        Background="#0d1117"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="CanResizeWithGrip">

    <Window.Resources>
        <!-- Nav item style -->
        <Style x:Key="NavItem" TargetType="ListBoxItem">
            <Setter Property="Foreground"        Value="#8b949e"/>
            <Setter Property="Background"        Value="Transparent"/>
            <Setter Property="Padding"           Value="12,10"/>
            <Setter Property="FontSize"          Value="12"/>
            <Setter Property="Cursor"            Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="6" Margin="4,2" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#21262d"/>
                                <Setter Property="Foreground" Value="#e6edf3"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#161b22"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary button -->
        <Style x:Key="PrimaryBtn" TargetType="Button">
            <Setter Property="Background"        Value="#1f6feb"/>
            <Setter Property="Foreground"        Value="White"/>
            <Setter Property="BorderThickness"   Value="0"/>
            <Setter Property="Padding"           Value="16,7"/>
            <Setter Property="FontSize"          Value="12"/>
            <Setter Property="Cursor"            Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#388bfd"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#1158c7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Ghost button -->
        <Style x:Key="GhostBtn" TargetType="Button">
            <Setter Property="Background"        Value="Transparent"/>
            <Setter Property="Foreground"        Value="#8b949e"/>
            <Setter Property="BorderBrush"       Value="#30363d"/>
            <Setter Property="BorderThickness"   Value="1"/>
            <Setter Property="Padding"           Value="16,7"/>
            <Setter Property="FontSize"          Value="12"/>
            <Setter Property="Cursor"            Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#21262d"/>
                                <Setter Property="Foreground" Value="#c9d1d9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Danger button -->
        <Style x:Key="DangerBtn" TargetType="Button">
            <Setter Property="Background"        Value="#da3633"/>
            <Setter Property="Foreground"        Value="White"/>
            <Setter Property="BorderThickness"   Value="0"/>
            <Setter Property="Padding"           Value="16,7"/>
            <Setter Property="FontSize"          Value="12"/>
            <Setter Property="Cursor"            Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#f85149"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#b62324"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Editor textbox -->
        <Style x:Key="NoteEditor" TargetType="TextBox">
            <Setter Property="Background"        Value="#0d1117"/>
            <Setter Property="Foreground"        Value="#c9d1d9"/>
            <Setter Property="BorderBrush"       Value="#30363d"/>
            <Setter Property="BorderThickness"   Value="1"/>
            <Setter Property="Padding"           Value="12,10"/>
            <Setter Property="FontSize"          Value="13"/>
            <Setter Property="CaretBrush"        Value="#c9d1d9"/>
            <Setter Property="TextWrapping"      Value="Wrap"/>
            <Setter Property="AcceptsReturn"     Value="True"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Body: nav list + editor -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="240"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left nav -->
            <Border Grid.Column="0" Background="#161b22"
                        BorderBrush="#30363d" BorderThickness="0,0,1,0">
                <DockPanel>
                    <Border DockPanel.Dock="Top" Padding="14,14,14,6">
                        <TextBlock Text="Notizen" Foreground="#e6edf3"
                                       FontSize="13" FontWeight="SemiBold"/>
                    </Border>
                    <Border DockPanel.Dock="Top" Padding="12,0,12,6">
                        <TextBlock x:Name="NavSubtitle" Text="" Foreground="#8b949e"
                                       FontSize="11" TextTrimming="CharacterEllipsis"/>
                    </Border>

                    <Border DockPanel.Dock="Top" Padding="10,6,10,10"
                            Background="#0d1117" CornerRadius="6" Margin="10,0,10,8">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Text="KI-Zusammenfassung" Grid.Row="0"
                                       Foreground="#8b949e" FontSize="11" FontWeight="SemiBold"
                                       Margin="0,0,0,6"/>
                            <TextBlock x:Name="KISummaryText" Grid.Row="1"
                                       Foreground="#c9d1d9" FontSize="11" TextWrapping="Wrap"
                                       MaxHeight="80" Text="Wird geladen..."/>
                            <ProgressBar x:Name="KISummaryProgress" Grid.Row="1"
                                         IsIndeterminate="True" Height="3"
                                         Background="Transparent" Foreground="#1f6feb"
                                         Visibility="Visible" VerticalAlignment="Top"
                                         Margin="0,0,0,4"/>
                        </Grid>
                    </Border>
                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled"
                                  DockPanel.Dock="Top">
                        <ListBox x:Name="NavList" Background="Transparent" BorderThickness="0"
                                 SelectionChanged="OnNavChanged" Padding="4,0">
                            <ListBox.ItemContainerStyle>
                                <Style BasedOn="{StaticResource NavItem}" TargetType="ListBoxItem">
                                    <Setter Property="ContentTemplate">
                                        <Setter.Value>
                                            <DataTemplate>
                                                <StackPanel>
                                                    <TextBlock Text="{Binding DatumUhrzeit}"
                                                               Foreground="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=Foreground}"
                                                               FontWeight="SemiBold" FontSize="12"/>
                                                    <TextBlock Text="{Binding TitelKurz}"
                                                               Foreground="#8b949e" FontSize="11"
                                                               TextTrimming="CharacterEllipsis" MaxWidth="190"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.ItemContainerStyle>
                        </ListBox>
                    </ScrollViewer>

                    <Button Content="+ Neue Notiz" Style="{StaticResource PrimaryBtn}"
                            DockPanel.Dock="Bottom" HorizontalAlignment="Stretch"
                            Click="OnNeueNotiz" Margin="10,0,10,10"/>
                </DockPanel>
            </Border>

            <!-- Right editor -->
            <Border Grid.Column="1" Background="#0d1117" Padding="16,14">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Notiz bearbeiten" Foreground="#e6edf3"
                               FontSize="14" FontWeight="SemiBold" Margin="0,0,0,10"/>

                    <TextBox x:Name="NoteEditorBox"
                             Grid.Row="1"
                             Style="{StaticResource NoteEditor}"
                             AcceptsReturn="True"
                             LostFocus="OnNoteEditorLostFocus"
                             TextChanged="OnNoteEditorTextChanged"/>
                </Grid>
            </Border>
        </Grid>

        <!-- Footer: actions -->
        <Border Grid.Row="1" Background="#161b22"
                BorderBrush="#30363d" BorderThickness="0,1,0,0"
                Padding="14,10">
            <DockPanel>
                <TextBlock x:Name="StatusLabel" DockPanel.Dock="Left"
                           Foreground="#3fb950" FontSize="12"
                           VerticalAlignment="Center" Visibility="Collapsed"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="DeleteBtn" Content="🗑 Löschen" Style="{StaticResource DangerBtn}"
                            Click="OnLoeschen" Margin="0,0,8,0" Visibility="Collapsed"/>
                    <Button Content="Speichern" Style="{StaticResource PrimaryBtn}"
                            Click="OnSave" Margin="0,0,8,0"/>
                    <Button Content="Schliessen" Style="{StaticResource GhostBtn}"
                            Click="OnClose"/>
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</Window>
